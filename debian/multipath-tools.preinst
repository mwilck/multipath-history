#!/bin/sh -e

kill_multipathd() {
   start-stop-daemon --stop --exec /usr/bin/multipathd --oknodo --quiet
}

case "$1" in
  install)
  ;;

  upgrade|abort-upgrade)
  kill_multipathd
  ;;

  *)
  echo "preinst called with unknown argument '$1'" >&2
  exit 1
  ;;
esac

#DEBHELPER#

