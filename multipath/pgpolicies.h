#include "vector.h"

void one_path_per_group (struct multipath *, char *);
void one_group (struct multipath *, char *);
void group_by_serial (struct multipath *, int, char *);
void group_by_tur (struct multipath *, char *);
